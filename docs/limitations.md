# Limitations

- Uses synthetic data; real deployment behavior may differ.
- Aggregate anomalies can arise from operations, not only leaks.
- Unsupervised models can produce false positives.
- Recommendation retrieval is local and intentionally lightweight.
- No direct control integration (no shutoff automation).
