# Demo Guide

1. Install dependencies.
2. Run `python tools/run_pipeline.py`.
3. Run `python tools/run_data_audit.py --run-pytest`.
4. Launch dashboard: `streamlit run app/dashboard.py`.
5. In dashboard, review KPIs, building comparisons, anomaly detail table, and responsible AI section.
